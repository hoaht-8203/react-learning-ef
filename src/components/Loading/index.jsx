import React from 'react';
import PropTypes from 'prop-types';
import './styles.css'

Loading.propTypes = {
    
};

function Loading(props) {
    return (
        <h1>LOADING...</h1>
    );
}

export default Loading;